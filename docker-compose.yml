services:
  bom_obs:
    image: "bom_observations"
    build: .
    container_name: "bom_obs"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped


volumes:
  bom_obs: {}
